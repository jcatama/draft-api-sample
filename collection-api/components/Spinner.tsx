const Spinner = ({}) => {
    return (
        <div
            className="w-12 h-12 rounded-full animate-spin
        border-4 border-solid border-t-transparent m-auto"
        ></div>
    );
};

export default Spinner;
